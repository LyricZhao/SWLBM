说明：
	1. 赛题中的网格规模(1000 * 500 * 500),算例,参数都不可更改.
	2. 赛题的总时间计时为 函数TIME_ST() 至 TIME_ED()之间的时间.
	3. 网格初始化,参数初始化,结果输出的部分进行了封装(不提供源码)
	   其不影响程序的优化, 为了各位参赛队员更好的理解程序,封装的程序做如下说明
	   SetParameter():     参数的初始化
	   INITINPUT():        对输入网格算例的初始化
	   TIME_ST():          计时开始
	   TIME_ED():          计时结束
	   OUTPUT():           输出部分
	   array*():  	       动态开辟空间                 
	4. 编译运行
	   编译: make 
	   运行: make run
	5. 结果说明
	   程序运行的结果在result.dat
	   程序计算部分结束后会进行结果校验
	   如果结果有误, 结果的错误文件在err.log(要求单精度绝对误差不大于1e-6)
	6. 每个参赛队配置8个节点以供程序优化与调试
	7. 每个参赛队最终的作业运行节点数不超过4个(节点数<=4)
	   

注：
	不允许修改Argument.h中的参数
	不允许修改Parallel.c的SetMPI函数实现	
	不允许修改计时区外的代码，计时区:MAIN CALCULATION SECTION
	可执行程序后边第一个参数必须为正确的用户名，否则无法记录成绩
	     
